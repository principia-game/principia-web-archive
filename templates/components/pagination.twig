{% set pages = (levels / lpp)|round(0, 'ceil') %}
{#{% if pages > 1 %}
<div class="pagination">
	{% for page in 1..pages -%}
		<span>{%- if page != current -%}
			<a href="{{ url | format(page) }}">{{ page }}</a>
		{%- else -%}
			{{ page }}
		{%- endif -%}</span>
	{%- endfor %}
</div>
{% endif %}#}

{% if pages > 1 %}
<div class="pagination2">
	{% if current != 1 %}
		<a href="{{ url | format(current-1) }}">&laquo; Previous</a>
	{% else %}
		<div></div>
	{% endif %}
	{% if current != pages %}
		<a href="{{ url | format(current+1) }}">Next &raquo;</a>
	{% else %}
		<div></div>
	{% endif %}
</div>
{% endif %}